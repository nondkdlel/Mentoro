import { css } from "styled-components";
import { ContentContainer } from "@style/theme";

export const FormWrapperStyle = css`
  padding: 50px 0 0;
`;

export const ServiceEnrollWrapperStyle = css`
  padding: 100px 0;
  ${ContentContainer}
`;
